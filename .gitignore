.env
.env.local
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
*.egg-info/

# local marketing assets — not meant for the public repo
marketing/

# local skill testing scratch area
testing/


# Playwright MCP session artifacts
.playwright-mcp/
/fk-*

# .claude/skills/* are meant to be real symlinks to skills/ (see CLAUDE.md),
# but this machine lacks symlink privilege so they're NTFS junctions, which
# git can't index. Ignored here; skills/ remains the source of truth.
.claude/skills/
